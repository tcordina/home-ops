apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
configMapGenerator:
  - name: alloy-config
    namespace: monitoring
    files:
      - config.alloy
generatorOptions:
  disableNameSuffixHash: true
