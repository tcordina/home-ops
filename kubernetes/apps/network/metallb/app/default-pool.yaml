---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: network
spec:
  addresses:
  - ${CLUSTER_EXT_IP}/32
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default-l2
  namespace: network
spec:
  ipAddressPools:
  - default-pool