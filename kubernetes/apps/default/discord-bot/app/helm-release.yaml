---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: discord-bot
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: default
  interval: 1h
  timeout: 1h
  values:
    controllers:
      bot:
        pod:
          securityContext:
            fsGroup: 1000
            fsGroupChangePolicy: "OnRootMismatch"
            runAsGroup: 1000
            runAsNonRoot: true
            runAsUser: 1000
        containers:
          app:
            image:
              repository: registry.gitlab.com/tcordina/discord-bot
              tag: latest
              digest: sha256:bd4b2d419a9a7b93bc4c95aff27750341d018e5f80df722ed0e32d3e2eb02f54 # @bot
              pullPolicy: IfNotPresent
            env: &env
              - name: LOG_LEVEL
                value: "INFO"
              - name: POSTGRES_HOST
                valueFrom:
                  secretKeyRef:
                    name: discord-bot-pguser-discord-bot
                    key: host
              - name: POSTGRES_PORT
                valueFrom:
                  secretKeyRef:
                    name: discord-bot-pguser-discord-bot
                    key: port
              - name: POSTGRES_USER
                valueFrom:
                  secretKeyRef:
                    name: discord-bot-pguser-discord-bot
                    key: user
              - name: POSTGRES_PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: discord-bot-pguser-discord-bot
                    key: password
              - name: POSTGRES_DB
                valueFrom:
                  secretKeyRef:
                    name: discord-bot-pguser-discord-bot
                    key: dbname
            envFrom:
              - secretRef:
                  name: discord-bot-secrets
              - secretRef:
                  name: default-auth-backblaze
            resources:
              requests:
                memory: "128Mi"
                cpu: "10m"
            securityContext: &securityContext
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
        initContainers:
          migrations:
            image:
              repository: registry.gitlab.com/tcordina/discord-bot
              tag: migrate-latest
              digest: sha256:d7921daeb8889ac88e6c9e5c29e9ed25da228176268ab5cfae3b568507ccc890 # @migrate
              pullPolicy: IfNotPresent
            command: ["uv", "run", "alembic", "upgrade", "head"]
            env: *env
            envFrom:
              - secretRef:
                  name: discord-bot-secrets
            securityContext: *securityContext
