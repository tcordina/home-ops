apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
  - helm-release.yaml
  - storage-class.yaml
## comment for now, no snapshot controller installed on k3s
#   - volume-snapshot-class.yaml
#   - ./jobs
