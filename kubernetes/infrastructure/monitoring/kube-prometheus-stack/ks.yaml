apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube-prometheus-stack
  namespace: flux-system
spec:
  interval: 5m
  path: ./kubernetes/infrastructure/monitoring/kube-prometheus-stack/app
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
