---
apiVersion: v1
kind: Namespace
metadata:
  name: immich
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: immich
  namespace: flux-system
spec:
  interval: 1h
  url: oci://ghcr.io/immich-app/immich-charts/immich
  ref:
    semver: "0.x"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: flux-system
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: immich
    namespace: flux-system
  targetNamespace: immich
  values:
    # Configuration du contrôleur principal
    controllers:
      main:
        containers:
          main:
            image:
              tag: v2.0.0
            env:
              REDIS_HOSTNAME: '{{ printf "%s-valkey" .Release.Name }}'
              # IMMICH_MACHINE_LEARNING_URL: '{{ printf "http://%s-machine-learning:3003" .Release.Name }}'
              # Variables DB si vous utilisez une DB externe
              DB_HOSTNAME: postgresql
              DB_PORT: "5432"
              DB_USERNAME: immich
              DB_PASSWORD: immich
              DB_DATABASE_NAME: immich
    
    # Configuration Immich
    immich:
      persistence:
        library:
          # Créer un PVC pour le stockage des photos
          existingClaim: immich-library
      
      # Métriques Prometheus (optionnel)
      metrics:
        enabled: false
    
    # Valkey (Redis fork) - cache pour les jobs
    valkey:
      enabled: true
      persistence:
        data:
          enabled: true
          size: 1Gi
          type: persistentVolumeClaim
          accessMode: ReadWriteOnce
          storageClass: local-path
    
    # Serveur Immich (API + Web)
    server:
      enabled: true
      ingress:
        main:
          enabled: true
          annotations:
            nginx.ingress.kubernetes.io/proxy-body-size: "0"
          hosts:
            - host: immich.lan.tablar.ovh
              paths:
                - path: /
                  service:
                    identifier: main
    
    # Machine Learning (reconnaissance faciale, objets, etc.)
    machine-learning:
      enabled: false
      # persistence:
      #   cache:
      #     enabled: true
      #     size: 10Gi
      #     type: persistentVolumeClaim
      #     accessMode: ReadWriteOnce
      #     storageClass: local-path